import { BubbleChartDirective } from './bubble-chart.directive';

describe('BubbleChartDirective', () => {
  it('should create an instance', () => {
    const directive = new BubbleChartDirective();
    expect(directive).toBeTruthy();
  });
});
