import { RadarChartDirective } from './radar-chart.directive';

describe('RadarChartDirective', () => {
  it('should create an instance', () => {
    const directive = new RadarChartDirective();
    expect(directive).toBeTruthy();
  });
});
